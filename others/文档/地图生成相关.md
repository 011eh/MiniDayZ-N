可以，找到了。核心不在 `c2runtime.js`，而是在 Construct 2 导出的事件表：[data.js](D:/others_projects/MiniDayZ/original_docs/data.js:1)。

关键位置：

- `project[6][0]` 是 `Game_events`
- `project[6][0][1][259]` 是顶层分组：`Map generation`
- 里面的主要函数名包括：
  `generate_array_locations`、`generate_array_roads_1`、`generate_array_roads_2`、`generate_array_tomap`、`generate_mapgen_toelements`、`generate_location`、`generate_minimap`、`generate_helicrashes`、`generate_bunker_entrance`、`Bunker_generate`

这些和加载文案能对上：[l_eng_ui.xml](D:/others_projects/MiniDayZ/original_docs/l_eng_ui.xml:86) 里有：

- `Generating world`
- `Placing roads (step 1)`
- `Placing roads (step 2)`
- `Adding secret places`
- `Tilemap drawings`
- `Creating minimap`

Tilemap 引擎实现本身在 [c2runtime.js](D:/others_projects/MiniDayZ/original_docs/c2runtime.js:24516)，但这是通用插件代码。真正的地图生成规则是在 `data.js` 的 `Map generation` 事件组里，通过 Construct 2 的 Function 调用和 Tilemap 对象操作完成。

相关 Tilemap 对象也能定位到：

- `t199` `images/tilemap_water.png`
- `t202` `images/tilemap_forest.png`
- `t221` `images/ground_enviroment_tilemap.png`
- `t411` `images/tilemap_sand.png`
- `t475/t681/t682/t771` `ground_lvl1~4_tilemap.png`
- `t779/t781` `tilemap_bunker.png` / `tilemap_bunker_collision.png`

结论：地图生成入口就是 `data.js` 的 `Game_events -> Map generation`，不是单独的 `.js` 函数源码文件。下一步可以把这个事件组从混淆 JSON 里单独提取成可读的伪代码。